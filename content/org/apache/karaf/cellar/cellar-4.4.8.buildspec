groupId=org.apache.karaf
artifactId=cellar
version=4.4.8

display=${groupId}:${artifactId}

gitRepo=https://github.com/apache/karaf-${artifactId}.git
gitTag=${artifactId}-${version}

tool=mvn
jdk=11
newline=lf

command="mvn -Prelease,deploy clean package -DskipTests -Dmaven.javadoc.skip -Dgpg.skip -Dbuildinfo.skipModules='*/manual'"
buildinfo=target/${artifactId}-${version}.buildinfo

#diffoscope=${artifactId}-${version}.diffoscope
issue=https://github.com/apache/karaf-cellar/pull/153
