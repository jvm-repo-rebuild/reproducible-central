groupId=io.jooby
artifactId=jooby
display=${groupId}:${artifactId}
version=3.11.0

gitRepo=https://github.com/jooby-project/${artifactId}.git
gitTag=v${version}

tool=mvn-3.9.9
jdk=17
newline=lf
#umask=022
#timezone="Europe/London"

command="mvn -Pbom,source clean package -DskipTests -Dmaven.javadoc.skip -Dbuildinfo.skipModules='*/tests'"
buildinfo=target/${artifactId}-project-${version}.buildinfo

#diffoscope=${artifactId}-project-${version}.diffoscope
issue=
