groupId=com.hibegin
artifactId=zrlog-base
display=${groupId}:${artifactId}
version=3.3.8

gitRepo=https://github.com/zrlog-extensions/zrlog-base
gitTag=v${version}
gitTag="git log --all --pretty=format:%H --grep 'release version $version'"

tool=mvn-3.9.12
jdk=21
newline=lf
#umask=022

command="mvn -Papache-release clean package -DskipTests -Dmaven.javadoc.skip -Dgpg.skip"
buildinfo=target/${artifactId}-${version}.buildinfo

#diffoscope=${artifactId}-${version}.diffoscope
issue=
