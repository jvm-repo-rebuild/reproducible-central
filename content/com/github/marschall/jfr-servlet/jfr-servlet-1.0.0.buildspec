groupId=com.github.marschall
artifactId=jfr-servlet
display=${groupId}:${artifactId}
version=1.0.0

gitRepo=https://github.com/marschall/jfr-servlet.git
gitTag=${version}

tool=mvn
# requires precisely 11.0.20
jdk=11
newline=lf
#umask=022

command="mvn clean package -DskipTests -Dmaven.javadoc.skip"
buildinfo=target/${artifactId}-${version}.buildinfo

#diffoscope=${artifactId}-${version}.diffoscope
issue=
